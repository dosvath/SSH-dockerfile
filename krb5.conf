[libdefaults]
    default_realm = $REALM
[realms]
	$REALM = {
			kdc_ports = 88
			kadmind_port = 749
			kdc = host.docker.internal
			admin_server = host.docker.internal
	}
[domain_realm]
        host.docker.internal = $REALM
